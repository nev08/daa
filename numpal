#include<stdio.h>
int main()
{
	int n,r,num=0;
	printf("enter n");
	scanf("%d",&n);
	int m=n;
	while(n!=0)
	{
		r=n%10;
		num=(num*10)+r;
		n=n/10;
	}
	printf("%d",num);
	if(m==num)
	{
		printf("palindrome");
	}
	else
	{
		printf("no");
	}
}
